# angee-examples

Showcase consumer addons for the Angee framework — reference third-party
addons built the folder-addon way (`addons/<namespace>/<name>/addon.toml`).
Consumed by a host project via one `ANGEE_ADDON_DIRS` line after cloning.

- `example.notes` — the notes demo addon the framework example host composes.
